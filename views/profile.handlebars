   <div class="card columns is-centered" id="profile-template">
      <div class="card-content">
        <div class="media">
          <div class="media-left">
            <figure class="image is-48x48">
              <img
                src="{{ image }}"
                alt="profile image"
              />
            </figure> 
          </div>
          <div class="media-content">
            <p class="title is-4">{{first_name}}</p>
            <p class="subtitle is-6">{{email}}</p>
          </div>
        </div>

        <div class="content">
          <p>Bio: {{bio}}</p>
          <br />          
        </div>
        
      </div>
      <div class="card-content">
        <div class="content">
          <h2 id="memeName">{{first_name}}'s meme</h2>
        </div>
      </div>

      <div class="card-content">
        {{#if memes.length}} 

        {{#each memes as |meme|}} 
        {{> profilePost meme}}
        <br />
        {{/each}} 
        
        {{/if}}
      </div>
    </div>
    {{#if (is_my_page user.id userId)}}
    <div class="container">
      <div class="card columns is-half">
        <a href="/create"
          ><button class="button is-success">Create a Post</button></a
        >
      </div>
      {{else}}

      <div class="card columns is-centered">
        <p>Hasn't created any posts yet!</p>
      </div>
    </div>
    {{/if}}

    <script src="/js/profile.js"></script>
 
